import personInfo

def draw():
    fill(20, 240, 20)
    textSize(20)
    text("Current cards", 400, 100)
    #help me with this
    if personInfo.strijdInv.len() > 0:
        for i in personInfo.strijdInv.len():
            if i == "plus5"
            text(personInfo.strijdInv[i], 300, (150 + (50 * i)))
    
